<resources>

    <!-- some old theme. -->
    <style name="AppTheme" parent="@style/Theme.AppCompat.Light.DarkActionBar">
    </style>

    <!-- Customize your theme here. -->
    <style name="Theme.AppTheme" parent="Base.Theme.AppTheme">
    </style>

    <!-- Base application theme -->
    <style name="Base.Theme.AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <item name="colorPrimary">#0091e9</item>
        <item name="colorPrimaryDark">#1976d2</item>
        <item name="colorAccent">#03a9f4</item>
        <item name="windowActionModeOverlay">true</item>
        <item name="android:actionModeBackground">@color/action_mode_color</item>
    </style>


    <!-- Argument Dialog style -->
    <style name="Theme.AppCompat.Translucent" parent="Base.Theme.AppTheme">
        <item name="android:windowBackground">@android:color/transparent</item>
        <item name="android:colorBackgroundCacheHint">@null</item>
        <item name="android:windowIsTranslucent">true</item>
    </style>


</resources>
